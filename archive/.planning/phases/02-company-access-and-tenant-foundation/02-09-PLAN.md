---
phase: 02-company-access-and-tenant-foundation
plan: 09
type: execute
wave: 9
depends_on:
  - "02-08"
files_modified:
  - frontend/src/api/index.ts
  - frontend/src/api/sdk.gen.ts
  - frontend/src/api/types.gen.ts
  - frontend/src/app/router.tsx
  - frontend/src/main.tsx
  - frontend/src/lib/clerk.ts
  - frontend/src/routes/auth/sign-in.tsx
  - frontend/src/routes/auth/sign-up.tsx
  - frontend/src/routes/auth/create-company.tsx
  - frontend/src/test/auth-routes.test.tsx
autonomous: true
gap_closure: true
requirements:
  - AUTH-01
  - AUTH-05
  - SETUP-01
user_setup: []
must_haves:
  truths:
    - Existing users can sign in and enter one local XAI Books post-auth decision path instead of hitting redirect loops or blank screens.
    - A first-time OAuth user can continue into account creation and company setup without raw Clerk recovery copy.
    - Company setup remains visible until backend bootstrap reports readiness instead of jumping prematurely into workspace.
    - Auth and onboarding screens materially match the approved owner-first split layout and copy contract.
  artifacts:
    - A single local Clerk adapter exists in `frontend/src/lib/clerk.ts`.
    - Generated frontend auth/bootstrap client files are refreshed for the new backend contract.
    - Sign-in, sign-up, and create-company routes implement one deterministic onboarding journey.
    - Router and provider redirects funnel through one local post-auth decision path.
  key_links:
    - `frontend/src/main.tsx` and `frontend/src/app/router.tsx` link Clerk fallback redirects to one product-owned decision path per D-16 and D-19.
    - `frontend/src/routes/auth/create-company.tsx` links to the generated bootstrap readiness client instead of implicit workspace redirects.
    - Runtime auth files link to `frontend/src/lib/clerk.ts` instead of scattered direct Clerk imports.
---

<objective>
Close the frontend auth and onboarding routing gaps without broadening Phase 2 beyond the already-approved UAT failures.

Purpose: once the backend exposes a typed readiness contract, the product still needs one deterministic sign-in, sign-up, and first-company journey that matches the approved XAI Books UI instead of leaking Clerk implementation details.
Output: a single Clerk adapter surface, refreshed generated bootstrap client files, unified post-auth routing, owner-first signed-out screens, and auth-route regression coverage.
</objective>

<execution_context>
@/Users/mali/Development/xai-bookkeeping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mali/Development/xai-bookkeeping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-company-access-and-tenant-foundation/02-CONTEXT.md
@.planning/phases/02-company-access-and-tenant-foundation/02-UAT.md
@.planning/phases/02-company-access-and-tenant-foundation/02-UI-SPEC.md
@.planning/phases/02-company-access-and-tenant-foundation/02-02-SUMMARY.md
@.planning/phases/02-company-access-and-tenant-foundation/02-04-SUMMARY.md
@xai-design.html
</context>

## Locked decisions carried forward

- `D-01` `D-02`: keep Clerk auth and httpOnly-cookie session handling; do not add JS token storage.
- `D-16` `D-17` `D-18` `D-19`: keep Clerk React integration, same-origin `/api` transport, and product-owned company context behavior.

<tasks>
<task type="auto">
  <name>Task 1: Centralize Clerk runtime usage and post-auth routing</name>
  <files>frontend/src/api/index.ts, frontend/src/api/sdk.gen.ts, frontend/src/api/types.gen.ts, frontend/src/app/router.tsx, frontend/src/main.tsx, frontend/src/lib/clerk.ts, frontend/src/test/auth-routes.test.tsx</files>
  <read_first>frontend/src/app/router.tsx, frontend/src/main.tsx, frontend/src/lib/clerk.ts, frontend/src/test/auth-routes.test.tsx, .planning/phases/02-company-access-and-tenant-foundation/02-UAT.md, .planning/phases/02-company-access-and-tenant-foundation/02-UI-SPEC.md</read_first>
  <action>Per `D-16` and `D-19`, keep the Clerk React stack but route runtime imports through `frontend/src/lib/clerk.ts` so unsupported named exports or version drift cannot blank-screen the app. Refresh the generated frontend client for the backend bootstrap contract, and change `ClerkProvider`, callback handling, and router fallbacks so every successful auth event lands in one local post-auth decision path instead of redirecting directly to `/workspace`. Cover the callback-path stability, blank-page regression, and bootstrap-client handoff behavior in `frontend/src/test/auth-routes.test.tsx`.</action>
  <verify>
    <automated>rtk make gen-types && rtk docker compose run --rm frontend npm run test -- --run src/test/auth-routes.test.tsx</automated>
  </verify>
  <done>The frontend has one stable Clerk adapter and one local post-auth routing path for Phase 2 auth transitions.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild sign-in, sign-up, and company-creation into one owner-first journey</name>
  <files>frontend/src/routes/auth/sign-in.tsx, frontend/src/routes/auth/sign-up.tsx, frontend/src/routes/auth/create-company.tsx, frontend/src/test/auth-routes.test.tsx</files>
  <read_first>frontend/src/routes/auth/sign-in.tsx, frontend/src/routes/auth/sign-up.tsx, frontend/src/routes/auth/create-company.tsx, frontend/src/test/auth-routes.test.tsx, .planning/phases/02-company-access-and-tenant-foundation/02-UAT.md, .planning/phases/02-company-access-and-tenant-foundation/02-UI-SPEC.md, xai-design.html</read_first>
  <action>Use the fixed split-screen login layout, copy hierarchy, and CTA contract from `02-UI-SPEC.md` and `xai-design.html` to turn the separate Clerk entry points into one coherent XAI Books journey. Existing-account sign-in must feel separate from missing-account continuation, but a first-time OAuth user who chooses Google must be guided into account creation and company setup without raw provider failure copy or user-blame language. Keep `CreateCompanyRoute` visible until the bootstrap contract reports readiness, and remove any automatic jump into workspace while setup is still pending. Extend `frontend/src/test/auth-routes.test.tsx` so it proves missing-account continuation, first-company handoff, and no-premature-workspace-redirect behavior.</action>
  <verify>
    <automated>rtk docker compose run --rm frontend npm run test -- --run src/test/auth-routes.test.tsx</automated>
  </verify>
  <done>The signed-out and first-run onboarding experience behaves like one intentional XAI Books flow and stays aligned with the approved UI contract.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Clerk browser session -> local route state | Signed-in browser state must still pass through the product-owned onboarding decision path |
| Generated bootstrap contract -> onboarding routes | Company setup cannot assume readiness before the backend says it is ready |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09-01 | Denial of Service | frontend auth routing | mitigate | Use one local Clerk adapter and one local post-auth decision path so unsupported exports or callback loops cannot blank-screen the app. |
| T-02-09-02 | Information Disclosure | signed-out and onboarding surfaces | mitigate | Replace raw provider errors with product-owned states and keep company setup blocked on the typed bootstrap readiness contract. |
</threat_model>

<verification>
- [ ] `rtk make gen-types`
- [ ] `rtk docker compose run --rm frontend npm run test -- --run src/test/auth-routes.test.tsx`
- [ ] `rtk make test-backend`
- [ ] `rtk make test-frontend`
</verification>

<success_criteria>
- Auth callbacks, redirects, and route guards no longer produce blank screens or sign-in loops.
- Missing-account OAuth attempts continue into the XAI Books account-creation and company-setup journey.
- Company setup remains visible until the backend bootstrap contract reports readiness.
- Signed-out screens match the approved split layout and owner-first copy contract.
</success_criteria>

<output>
Create `.planning/phases/02-company-access-and-tenant-foundation/02-09-SUMMARY.md` when done
</output>
