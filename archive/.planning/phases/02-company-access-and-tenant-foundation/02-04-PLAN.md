---
phase: 02-company-access-and-tenant-foundation
plan: 04
type: execute
wave: 4
depends_on:
  - "02-03"
files_modified:
  - frontend/src/routes/root.tsx
  - frontend/src/routes/workspace/index.tsx
  - frontend/src/components/molecules/company-switcher.tsx
  - frontend/src/components/organisms/permission-denied-state.tsx
  - frontend/src/api/
  - frontend/src/test/company-switcher.test.tsx
autonomous: true
requirements:
  - COMP-02
  - COMP-04
  - SETUP-01
user_setup: []
must_haves:
  truths:
    - Users can switch between authorized companies from the app shell.
    - Eligible users can create an additional company from the same company switcher flow.
    - Company switching clears cached data from the previous company before new company content renders.
    - Denied company access is surfaced calmly and explicitly in the UI.
  artifacts:
    - Header company switcher and permission-denied components exist.
    - Workspace shell consumes generated company-scoped API clients.
  key_links:
    - Active organization switching is linked to company-scoped generated API calls.
    - Add-company UI is linked to Clerk organization creation and a safe newly-created-company activation flow.
---

<objective>
Build the frontend tenant shell: company switching, denied-state handling, and company-scoped generated-client integration.

Purpose: once the backend tenant boundary exists, the app shell needs a safe way to switch companies without leaking stale data or hiding authorization failures.
Output: custom company switcher, add-company flow, permission-denied UI, regenerated company-scoped client usage, and frontend switching tests.
</objective>

<execution_context>
@/Users/mali/Development/xai-bookkeeping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mali/Development/xai-bookkeeping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-company-access-and-tenant-foundation/02-UI-SPEC.md
@frontend/src/routes/root.tsx
@frontend/src/routes/workspace/index.tsx
@xai-design.html
</context>

<tasks>
<task type="auto">
  <name>Task 1: Build the company switcher and company-scoped shell behavior</name>
  <files>frontend/src/routes/root.tsx, frontend/src/routes/workspace/index.tsx, frontend/src/components/molecules/company-switcher.tsx, frontend/src/components/organisms/permission-denied-state.tsx, frontend/src/api/, frontend/src/test/company-switcher.test.tsx</files>
  <read_first>frontend/src/routes/root.tsx, frontend/src/routes/workspace/index.tsx, frontend/src/components/molecules/company-switcher.tsx, frontend/src/components/organisms/permission-denied-state.tsx, frontend/src/api/, frontend/src/test/company-switcher.test.tsx, .planning/phases/02-company-access-and-tenant-foundation/02-UI-SPEC.md, xai-design.html</read_first>
  <action>Create the header company switcher as a custom Clerk organization switch flow using `useOrganizationList()` and `setActive(...)`, include an add-company action that launches Clerk organization creation for users allowed to start another workspace, consume the regenerated company-scoped API client methods from `frontend/src/api/`, and render explicit loading/no-access states while switching. After company creation or switching, clear TanStack Query cache immediately so previous-company data never lingers on screen and land the user in a safe newly-active-company workspace state. Keep permission-denied messaging aligned with the approved UI contract. Do not depend on the settings UI being present yet; that completion flow arrives in later plans.</action>
  <verify>
    <automated>rtk make gen-types && rtk docker compose run --rm frontend npm run test -- --run src/test/company-switcher.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/molecules/company-switcher.tsx` uses Clerk organization switching primitives
    - the switcher exposes an add-company affordance tied to Clerk organization creation
    - `frontend/src/routes/root.tsx` renders the switcher in the app header
    - previous-company data is cleared during switching
    - generated client code in `frontend/src/api/` is refreshed and used by the shell
  </acceptance_criteria>
  <done>The frontend can switch between authorized companies and create additional companies without visually leaking previous-company data.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
- Cached frontend company data vs. newly activated company context

## STRIDE Register
| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Information Disclosure | Frontend company switching | mitigate | Clear query cache on successful switch and replace scoped content with loading/denied states during transition. |
</threat_model>

## Artifacts this phase produces
- `frontend/src/components/molecules/company-switcher.tsx`
  - `CompanySwitcher`
- `frontend/src/components/organisms/permission-denied-state.tsx`
  - permission-denied UI state

<verification>
Before declaring plan complete:
- [ ] `rtk make gen-types`
- [ ] `rtk docker compose run --rm frontend npm run test -- --run src/test/company-switcher.test.tsx`
- [ ] `rtk make test-backend && rtk make test-frontend`
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Users can switch between authorized companies and start an additional company safely
</success_criteria>

<output>
After completion, create `.planning/phases/02-company-access-and-tenant-foundation/02-04-SUMMARY.md`
</output>
