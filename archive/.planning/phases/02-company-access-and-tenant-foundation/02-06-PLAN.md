---
phase: 02-company-access-and-tenant-foundation
plan: 06
type: execute
wave: 6
depends_on:
  - "02-05"
files_modified:
  - backend/app/db/models/company.py
  - backend/app/db/models/audit_event.py
  - backend/app/db/models/__init__.py
  - backend/app/audit/service.py
  - backend/app/platform/permissions.py
  - backend/app/api/routes/company_settings.py
  - backend/app/api/routes/audit.py
  - backend/app/api/routes/auth.py
  - backend/app/api/routes/team.py
  - backend/app/api/router.py
  - backend/migrations/versions/0005_phase2_audit_settings.py
  - backend/tests/test_company_settings.py
  - backend/tests/test_audit_events.py
autonomous: true
requirements:
  - SETUP-01
  - SETUP-02
  - SETUP-03
  - SETUP-04
  - AUDT-01
  - AUDT-02
  - AUDT-04
  - AUTH-04
user_setup: []
must_haves:
  truths:
    - UAE company settings are validated and persisted with AED, TRN, VAT registration status, and 5% default VAT semantics.
    - Important Phase 2 actions write company-scoped audit rows with actor, entity, action, timestamp, and before/after detail.
    - Unauthorized users cannot read or mutate another company's settings or audit rows, and deny behavior is verified by role.
  artifacts:
    - Append-only audit event model and same-transaction audit service exist.
    - Company settings and audit endpoints exist in the backend.
    - Existing auth/team mutation paths are retrofitted to emit Phase 2 audit events, including member removal.
  key_links:
    - Settings writes are linked to audit-event creation in the same transaction.
    - `settings:update` and `audit:view` permissions are linked to backend guards and deny-path tests.
---

<objective>
Finish the backend trust layer with audited UAE company settings and explicit permission enforcement on settings and audit resources.

Purpose: Phase 2 is not trustworthy until critical platform actions are audited, company profile data is validated with UAE-first defaults, and role-based access to settings/audit surfaces is verifiably enforced.
Output: audit-event model and service, company settings endpoints, audit-history endpoint, explicit settings/audit permission guards, and backend settings/audit tests.
</objective>

<execution_context>
@/Users/mali/Development/xai-bookkeeping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mali/Development/xai-bookkeeping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-company-access-and-tenant-foundation/02-CONTEXT.md
@backend/app/api/routes/auth.py
@backend/app/api/routes/team.py
@backend/app/platform/company_access.py
@backend/app/platform/permissions.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create the audit-event model, service, and company-scoped audit reads</name>
  <files>backend/app/db/models/audit_event.py, backend/app/db/models/__init__.py, backend/app/audit/service.py, backend/app/api/routes/audit.py, backend/app/api/routes/auth.py, backend/app/api/routes/team.py, backend/app/api/router.py, backend/migrations/versions/0005_phase2_audit_settings.py, backend/tests/test_audit_events.py</files>
  <read_first>backend/app/db/models/audit_event.py, backend/app/audit/service.py, backend/app/api/routes/audit.py, backend/app/api/routes/auth.py, backend/app/api/routes/team.py, backend/tests/test_audit_events.py, .planning/phases/02-company-access-and-tenant-foundation/02-CONTEXT.md</read_first>
  <action>Create the append-only `AuditEvent` model with company scope plus before/after JSONB change detail, implement a same-transaction audit write helper, add company-scoped audit-history reads, and patch the existing auth/team mutation paths so login-related success, invitation creation/revocation, role changes, and member removal emit audit rows. Tests must prove audit rows are company-scoped, readable only through authorized company access, and created atomically with the triggering mutation.</action>
  <verify>
    <automated>rtk docker compose run --rm backend pytest -q tests/test_audit_events.py && rtk docker compose run --rm backend alembic upgrade head</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/db/models/audit_event.py` exists with company-scoped audit fields
    - `backend/app/audit/service.py` writes in the same transaction as the business action
    - `backend/app/api/routes/audit.py` exposes company-scoped audit reads
    - `backend/tests/test_audit_events.py` proves atomic audited writes and scoped reads, including member-removal events
  </acceptance_criteria>
  <done>The backend has a reusable, company-scoped audit foundation and existing Phase 2 auth/team mutations, including member removal, emit audit rows.</done>
</task>

<task type="auto">
  <name>Task 2: Add audited UAE company settings endpoints with explicit role-based guards</name>
  <files>backend/app/db/models/company.py, backend/app/platform/permissions.py, backend/app/api/routes/company_settings.py, backend/app/api/routes/audit.py, backend/app/api/router.py, backend/tests/test_company_settings.py, backend/tests/test_audit_events.py</files>
  <read_first>backend/app/db/models/company.py, backend/app/platform/permissions.py, backend/app/api/routes/company_settings.py, backend/app/api/routes/audit.py, backend/app/api/router.py, backend/tests/test_company_settings.py, backend/tests/test_audit_events.py, .planning/phases/02-company-access-and-tenant-foundation/02-CONTEXT.md, .planning/phases/02-company-access-and-tenant-foundation/02-UI-SPEC.md</read_first>
  <action>Add company settings read/update endpoints and wire them to explicit permission checks such as `settings:update`; wire audit-history reads to explicit `audit:view` checks. Support editable legal name, business activity, TRN, VAT registration status, registered address, fixed `AED` currency, and default `5.00` VAT behavior. Extend tests so they verify not only validation and audit writes but also allow/deny behavior by role for settings update and audit-history access.</action>
  <verify>
    <automated>rtk docker compose run --rm backend pytest -q tests/test_company_settings.py tests/test_audit_events.py</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/api/routes/company_settings.py` uses explicit role-based permission guards
    - `backend/app/api/routes/audit.py` uses explicit role-based permission guards
    - validation and defaults cover AED, TRN, VAT registration status, and 5% VAT
    - backend tests prove allow/deny behavior for settings and audit resources by role
  </acceptance_criteria>
  <done>The backend can store UAE-first company settings, audit every settings change, and verifiably enforce role-based access to settings and audit resources.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
- Company-scoped mutations vs. audit persistence
- Authorized company users vs. unauthorized settings/audit readers

## STRIDE Register
| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | Repudiation | Audit-event persistence | mitigate | Write audit rows in the same transaction as the triggering action and expose only company-scoped audit reads. |
| T-02-12 | Tampering | Company settings and audit access | mitigate | Enforce explicit settings/audit permissions in the backend and verify allow/deny behavior by role. |
</threat_model>

## Artifacts this phase produces
- `backend/app/db/models/audit_event.py`
  - `AuditEvent`
- `backend/app/audit/service.py`
  - same-transaction audit helper
- `backend/app/api/routes/company_settings.py`
  - company settings endpoints
- `backend/app/api/routes/audit.py`
  - audit history endpoints

<verification>
Before declaring plan complete:
- [ ] `rtk docker compose run --rm backend pytest -q tests/test_company_settings.py tests/test_audit_events.py`
- [ ] `rtk docker compose run --rm backend alembic upgrade head`
- [ ] `rtk make test-backend && rtk make test-frontend`
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- UAE company settings are validated and audited in the backend
- Settings and audit resources have explicit, tested role-based access control
</success_criteria>

<output>
After completion, create `.planning/phases/02-company-access-and-tenant-foundation/02-06-SUMMARY.md`
</output>
