---
phase: 02-company-access-and-tenant-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - backend/app/db/models/company.py
  - backend/app/db/models/company_membership.py
  - backend/app/db/models/__init__.py
  - backend/app/platform/webhooks.py
  - backend/app/platform/company_access.py
  - backend/app/api/routes/companies.py
  - backend/app/api/router.py
  - backend/migrations/versions/0003_phase2_companies.py
  - backend/tests/test_company_authorization.py
autonomous: true
requirements:
  - SETUP-01
  - COMP-01
  - COMP-02
  - COMP-03
  - COMP-04
  - COMP-05
user_setup: []
must_haves:
  truths:
    - Users can belong to more than one company and the backend can resolve authorized company context.
    - Clerk organization creation materializes the initial local company/profile row instead of assuming companies already exist.
    - Backend rejects every foreign-company API request with 403.
  artifacts:
    - Company and company-membership shadow tables exist.
    - Company-scoped backend dependency and route tree exist.
  key_links:
    - Clerk organization/membership webhooks are linked to local company shadow rows.
    - Company-scoped routes are linked to the reusable backend tenant guard.
---

<objective>
Implement the backend tenant boundary: local company mirrors, webhook-driven membership sync, and company-scoped authorization.

Purpose: the rest of Phase 2 is meaningless unless the backend can prove which company is active and reject all cross-company access before any frontend shell behavior is added.
Output: company/membership models and sync, initial company bootstrap on organization creation, company authorization dependency, company-scoped routes, and negative authorization tests.
</objective>

<execution_context>
@/Users/mali/Development/xai-bookkeeping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mali/Development/xai-bookkeeping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-company-access-and-tenant-foundation/02-CONTEXT.md
@.planning/phases/02-company-access-and-tenant-foundation/02-RESEARCH.md
@backend/app/platform/webhooks.py
@backend/app/api/router.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Mirror companies and memberships and enforce company-scoped backend access</name>
  <files>backend/app/db/models/company.py, backend/app/db/models/company_membership.py, backend/app/db/models/__init__.py, backend/app/platform/webhooks.py, backend/app/platform/company_access.py, backend/app/api/routes/companies.py, backend/app/api/router.py, backend/migrations/versions/0003_phase2_companies.py, backend/tests/test_company_authorization.py</files>
  <read_first>backend/app/db/models/company.py, backend/app/db/models/company_membership.py, backend/app/platform/webhooks.py, backend/app/platform/company_access.py, backend/app/api/routes/companies.py, backend/tests/test_company_authorization.py, .planning/phases/02-company-access-and-tenant-foundation/02-CONTEXT.md</read_first>
  <action>Create company and company-membership shadow tables keyed to Clerk organizations and memberships, extend the verified webhook sync path to handle `organization.created`, `organization.updated`, `organization.deleted`, `organizationMembership.created`, `organizationMembership.updated`, and `organizationMembership.deleted`, and add the reusable `get_current_company`-style backend dependency for `/api/v1/companies/{company_id}/...` routes. `organization.created` must bootstrap the initial local company/profile row so the newly created company becomes selectable immediately, while `organizationMembership.updated` must sync Clerk role changes back into the local membership mirror. `organization.deleted` must archive or tombstone the local company shadow row safely so stale companies cannot remain selectable. Tests must prove same-company access succeeds, foreign-company access returns 403, and membership create/update/delete events update the local shadow rows deterministically.</action>
  <verify>
    <automated>rtk docker compose run --rm backend pytest -q tests/test_company_authorization.py && rtk docker compose run --rm backend alembic upgrade head</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/db/models/company.py` and `backend/app/db/models/company_membership.py` exist
    - `backend/app/platform/company_access.py` provides a reusable company-resolution dependency
    - `backend/app/platform/webhooks.py` syncs organization create/update/delete plus membership create/update/delete events
    - initial Clerk organization creation produces the local company/profile row required for first-run setup
    - `backend/tests/test_company_authorization.py` proves cross-company requests return 403
  </acceptance_criteria>
  <done>Backend company scope is modeled locally, initial company creation is materialized from Clerk org events, and tenant enforcement is consistent.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
- Clerk organization/membership truth vs. local XAI shadow rows
- Authenticated user context vs. requested `company_id`
- Cached frontend company data vs. newly activated company context

## STRIDE Register
| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Tampering | Company/membership shadow sync | mitigate | Verify organization/membership webhooks and keep local shadow-row updates idempotent. |
| T-02-07 | Information Disclosure | Company-scoped backend routes | mitigate | Enforce company resolution and membership checks centrally, returning 403 on any foreign-company access attempt. |
| T-02-08 | Information Disclosure | Frontend company switching | mitigate | Clear query cache on successful switch and replace scoped content with loading/denied states during transition. |
</threat_model>

## Artifacts this phase produces
- `backend/app/db/models/company.py`
  - `Company`
- `backend/app/db/models/company_membership.py`
  - `CompanyMembership`
- `backend/app/platform/company_access.py`
  - company-resolution dependency

<verification>
Before declaring plan complete:
- [ ] `rtk docker compose run --rm backend pytest -q tests/test_company_authorization.py`
- [ ] `rtk docker compose run --rm backend alembic upgrade head`
- [ ] `rtk make test-backend && rtk make test-frontend`
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Backend rejects cross-company access attempts with 403
</success_criteria>

<output>
After completion, create `.planning/phases/02-company-access-and-tenant-foundation/02-03-SUMMARY.md`
</output>
