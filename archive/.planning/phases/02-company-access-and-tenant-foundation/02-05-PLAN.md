---
phase: 02-company-access-and-tenant-foundation
plan: 05
type: execute
wave: 5
depends_on:
  - "02-03"
  - "02-04"
files_modified:
  - backend/app/db/models/role_permission.py
  - backend/app/db/models/company_membership.py
  - backend/app/db/models/__init__.py
  - backend/app/platform/permissions.py
  - backend/app/platform/clerk_organizations.py
  - backend/app/api/routes/team.py
  - backend/app/api/router.py
  - backend/migrations/versions/0004_phase2_permissions.py
  - backend/tests/test_role_permissions.py
  - frontend/src/components/atoms/role-badge.tsx
  - frontend/src/components/organisms/invite-member-dialog.tsx
  - frontend/src/routes/workspace/team/index.tsx
  - frontend/src/api/
  - frontend/src/test/team-roles.test.tsx
autonomous: true
requirements:
  - AUTH-02
  - AUTH-03
  - AUTH-04
user_setup: []
must_haves:
  truths:
    - Authorized company admins and owners can invite members and change roles through Clerk Organizations.
    - Authorized company admins and owners can remove members and revoke pending invites through the product UI.
    - Unauthorized roles are denied by the backend through a reusable fixed-role permission foundation that also covers future company resources, not just current screens.
    - Team-management UI exposes human-readable role semantics and denied-action states.
  artifacts:
    - Role-permission persistence exists in Postgres.
    - Clerk-backed team-management integration exists in the backend.
    - Team & Roles UI exists and consumes generated client methods.
  key_links:
    - Clerk invitation and membership-update APIs are linked to webhook-driven shadow-table sync.
    - Permission rows for broad action families like `view`, `create`, `edit`, `approve`, `delete`, `export`, and `configure` are linked to backend enforcement, with explicit Phase 2 coverage for `users:*`, `settings:update`, and `audit:view`.
---

<objective>
Add fixed roles, backend-enforced permissions, and Clerk-backed team-management flows.

Purpose: Phase 2 access requirements only hold if invitations and membership role changes go through Clerk Organizations while XAI enforces its own company-scoped authorization model on top.
Output: role-permission model, Clerk org integration helpers, team endpoints, Team & Roles UI, regenerated typed client methods, and role-based negative tests.

Scope note: this plan intentionally keeps the permission matrix, Clerk-backed team lifecycle, and Team & Roles UI in one slice because they share the same team endpoint contract, generated client refresh, and regression surface.
</objective>

<execution_context>
@/Users/mali/Development/xai-bookkeeping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mali/Development/xai-bookkeeping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-company-access-and-tenant-foundation/02-CONTEXT.md
@.planning/phases/02-company-access-and-tenant-foundation/02-UI-SPEC.md
@backend/app/platform/company_access.py
@backend/app/platform/webhooks.py
@frontend/src/routes/root.tsx
@xai-design.html
</context>

<tasks>
<task type="auto">
  <name>Task 1: Define the fixed permission matrix as the company authorization foundation</name>
  <files>backend/app/db/models/role_permission.py, backend/app/db/models/company_membership.py, backend/app/db/models/__init__.py, backend/app/platform/permissions.py, backend/migrations/versions/0004_phase2_permissions.py, backend/tests/test_role_permissions.py</files>
  <read_first>backend/app/db/models/role_permission.py, backend/app/db/models/company_membership.py, backend/app/platform/permissions.py, backend/migrations/versions/0004_phase2_permissions.py, backend/tests/test_role_permissions.py, .planning/phases/02-company-access-and-tenant-foundation/02-CONTEXT.md</read_first>
  <action>Create the fixed `Owner`, `Admin`, `Accountant`, and `Viewer` permission matrix in Postgres using reusable `resource:action` permissions as the company authorization foundation for later phases. Define the matrix broadly enough to express common action families such as `view`, `create`, `edit`, `approve`, `delete`, `export`, and `configure`, while explicitly seeding the concrete Phase 2 permissions `users:invite`, `users:update-role`, `users:remove`, `settings:update`, and `audit:view`. Add a reusable permission dependency that composes the authenticated principal with the current company and extend the role-permission tests so they prove allow/deny behavior for those settings and audit actions as well as membership actions.</action>
  <verify>
    <automated>rtk docker compose run --rm backend pytest -q tests/test_role_permissions.py && rtk docker compose run --rm backend alembic upgrade head</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/platform/permissions.py` defines reusable company permission enforcement
    - the seeded permission matrix is documented as a reusable fixed-role foundation, not a team-only special case
    - the seeded permission matrix includes explicit settings and audit permissions
    - `backend/tests/test_role_permissions.py` covers allow/deny behavior for settings and audit permissions in addition to membership permissions
  </acceptance_criteria>
  <done>The backend has a fixed-role permission foundation that spans all Phase 2 protected resources and can carry forward into later modules.</done>
</task>

<task type="auto">
  <name>Task 2: Wire team-management routes to Clerk Organizations and webhook-backed shadow sync</name>
  <files>backend/app/platform/clerk_organizations.py, backend/app/api/routes/team.py, backend/app/api/router.py, backend/tests/test_role_permissions.py</files>
  <read_first>backend/app/platform/clerk_organizations.py, backend/app/api/routes/team.py, backend/app/platform/webhooks.py, backend/tests/test_role_permissions.py, .planning/phases/02-company-access-and-tenant-foundation/02-CONTEXT.md</read_first>
  <action>Create a backend integration helper for Clerk organization invitations and membership updates, then revise the team-management endpoints so invitation, invite revocation, role change, and member removal operations all call Clerk Organization APIs instead of mutating local-only state. Persist role metadata in Clerk membership records, and verify in tests that Clerk membership change events, especially membership role updates, flow back through webhook handling so the local shadow tables remain webhook-driven mirrors of Clerk state rather than the source of truth for membership lifecycle actions. The verify path must prove both pending-invite revocation and existing-member removal end to end.</action>
  <verify>
    <automated>rtk docker compose run --rm backend pytest -q tests/test_role_permissions.py</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/platform/clerk_organizations.py` exists and owns Clerk org invitation/membership operations
    - `backend/app/api/routes/team.py` calls Clerk org APIs for invite, invite-revocation, role-change, and member-removal flows
    - tests prove Clerk membership role changes are synced into the local mirror through webhook handling
    - tests prove both pending invites can be revoked and active members can be removed while webhook-driven shadow sync remains the authoritative local reflection of Clerk membership lifecycle changes
  </acceptance_criteria>
  <done>Team-management actions now operate through Clerk Organizations while preserving local authorization and shadow-table sync.</done>
</task>

<task type="auto">
  <name>Task 3: Build the Team & Roles UI on generated team endpoints</name>
  <files>frontend/src/components/atoms/role-badge.tsx, frontend/src/components/organisms/invite-member-dialog.tsx, frontend/src/routes/workspace/team/index.tsx, frontend/src/api/, frontend/src/test/team-roles.test.tsx</files>
  <read_first>frontend/src/components/atoms/role-badge.tsx, frontend/src/components/organisms/invite-member-dialog.tsx, frontend/src/routes/workspace/team/index.tsx, frontend/src/api/, frontend/src/test/team-roles.test.tsx, .planning/phases/02-company-access-and-tenant-foundation/02-UI-SPEC.md, xai-design.html</read_first>
  <action>Create the Team & Roles route, role badges, invite-member dialog, role-assignment controls, and member/invite row actions using the approved density and copy contract from `02-UI-SPEC.md`. Consume regenerated client methods from `frontend/src/api/` instead of manual fetch code. Tests must cover invite flow, role labels, role changes, member removal, invite revocation, and denied-action messaging for unauthorized roles.</action>
  <verify>
    <automated>rtk make gen-types && rtk docker compose run --rm frontend npm run test -- --run src/test/team-roles.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/routes/workspace/team/index.tsx` renders the Team & Roles screen
    - generated client methods in `frontend/src/api/` are refreshed and used
    - `frontend/src/test/team-roles.test.tsx` covers invite, role-change, member-removal, invite-revocation, and denied team-management states
  </acceptance_criteria>
  <done>The frontend exposes Clerk-backed invite, role-assignment, member-removal, and invite-revocation flows on typed API contracts.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
- Local permission model vs. Clerk-owned membership lifecycle
- Authorized admins/owners vs. lower-role users attempting membership or configuration actions

## STRIDE Register
| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Elevation of Privilege | Backend permission enforcement | mitigate | Enforce explicit permission rows for membership, settings, and audit resources at the backend boundary. |
| T-02-10 | Spoofing | Clerk-backed team-management routes | mitigate | Require same-company authenticated admins/owners and send invite/role-change operations through Clerk Organization APIs, not local-only mutations. |
</threat_model>

## Artifacts this phase produces
- `backend/app/platform/clerk_organizations.py`
  - Clerk org invitation/membership helper
- `backend/app/db/models/role_permission.py`
  - `RolePermission`
- `backend/app/platform/permissions.py`
  - company permission dependency
- `frontend/src/components/organisms/invite-member-dialog.tsx`
  - `InviteMemberDialog`
- `frontend/src/routes/workspace/team/index.tsx`
  - Team & Roles route

<verification>
Before declaring plan complete:
- [ ] `rtk docker compose run --rm backend pytest -q tests/test_role_permissions.py`
- [ ] `rtk docker compose run --rm backend alembic upgrade head`
- [ ] `rtk make gen-types`
- [ ] `rtk docker compose run --rm frontend npm run test -- --run src/test/team-roles.test.tsx`
- [ ] `rtk make test-backend && rtk make test-frontend`
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Clerk Organization APIs back the invitation, revocation, role-change, and member-removal lifecycle
- Team-management UI supports invite, role-change, member-removal, and invite-revocation flows end-to-end
- Phase 2 permissions concretely cover team, settings, and audit resources while establishing the fixed-role authorization foundation for later modules
</success_criteria>

<output>
After completion, create `.planning/phases/02-company-access-and-tenant-foundation/02-05-SUMMARY.md`
</output>
