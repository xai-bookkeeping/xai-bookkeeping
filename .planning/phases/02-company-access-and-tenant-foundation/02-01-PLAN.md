---
phase: 02-company-access-and-tenant-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pyproject.toml
  - backend/app/core/config.py
  - backend/app/db/session.py
  - backend/migrations/env.py
  - backend/app/platform/auth.py
  - backend/app/platform/webhooks.py
  - backend/app/db/models/user.py
  - backend/app/db/models/__init__.py
  - backend/app/api/routes/auth.py
  - backend/app/api/router.py
  - backend/migrations/versions/0002_phase2_auth_users.py
  - backend/tests/test_auth.py
autonomous: true
requirements:
  - AUTH-01
  - AUTH-05
user_setup:
  - Configure Clerk development keys and webhook signing secret in the local environment before running real integration checks.
must_haves:
  truths:
    - Protected backend routes reject unauthenticated requests with 401.
    - Authenticated backend routes resolve a stable local user principal keyed by Clerk user ID.
    - Clerk user webhooks can upsert local user shadow rows and reject unsigned payloads.
  artifacts:
    - Async SQLAlchemy request-session infrastructure exists.
    - Clerk auth verification and user-webhook sync code exist in `backend/app/platform/`.
    - A local user shadow table and authenticated `/api/v1/auth/me` route exist.
  key_links:
    - Clerk-authenticated request state is linked to FastAPI authorization.
    - Clerk `user.created`, `user.updated`, and `user.deleted` events are linked to local `users` shadow rows.
---

<objective>
Build the backend identity foundation for Phase 2: async DB sessions, Clerk request verification, user shadow persistence, and verified user-webhook sync.

Purpose: later company, role, settings, and audit work all depend on a backend that can trust Clerk identity and resolve a local user record without blocking the event loop.
Output: async DB session migration, Clerk settings/dependencies, user shadow model/migration, verified auth dependency, verified user webhook sync path including deletion handling, and backend auth tests.
</objective>

<execution_context>
@/Users/mali/Development/xai-bookkeeping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mali/Development/xai-bookkeeping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-company-access-and-tenant-foundation/02-CONTEXT.md
@.planning/phases/02-company-access-and-tenant-foundation/02-RESEARCH.md
@backend/pyproject.toml
@backend/app/core/config.py
@backend/app/db/session.py
@backend/migrations/env.py
@backend/app/api/router.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Convert backend runtime auth and DB access to the Phase 2 async foundation</name>
  <files>backend/pyproject.toml, backend/app/core/config.py, backend/app/db/session.py, backend/migrations/env.py, backend/app/platform/auth.py</files>
  <read_first>backend/pyproject.toml, backend/app/core/config.py, backend/app/db/session.py, backend/migrations/env.py, backend/app/platform/auth.py, .planning/phases/02-company-access-and-tenant-foundation/02-CONTEXT.md, .planning/phases/02-company-access-and-tenant-foundation/02-RESEARCH.md</read_first>
  <action>Add `clerk-backend-api` and `asyncpg`, extend backend settings with Clerk keys, authorized parties, and webhook signing secret, replace the sync request session path with `create_async_engine(...)` plus `async_sessionmaker(...)`, and implement `backend/app/platform/auth.py` as the single Clerk verification entry point. Verify only `session_token`, support local verification through `CLERK_JWT_KEY` when present, and return a stable authenticated principal that includes Clerk user ID and active organization ID.</action>
  <verify>
    <automated>rtk docker compose run --rm backend pytest -q tests/test_auth.py</automated>
  </verify>
  <acceptance_criteria>
    - `backend/pyproject.toml` contains `clerk-backend-api` and `asyncpg`
    - `backend/app/db/session.py` uses `create_async_engine` and `async_sessionmaker`
    - `backend/app/platform/auth.py` verifies Clerk-authenticated request state for protected routes
    - `backend/migrations/env.py` remains compatible with `alembic upgrade head`
  </acceptance_criteria>
  <done>Backend runtime auth and DB access are async and Clerk-aware.</done>
</task>

<task type="auto">
  <name>Task 2: Add the user shadow table, auth route, and verified user webhook sync</name>
  <files>backend/app/platform/webhooks.py, backend/app/db/models/user.py, backend/app/db/models/__init__.py, backend/app/api/routes/auth.py, backend/app/api/router.py, backend/migrations/versions/0002_phase2_auth_users.py, backend/tests/test_auth.py</files>
  <read_first>backend/app/platform/webhooks.py, backend/app/db/models/user.py, backend/app/db/models/__init__.py, backend/app/api/routes/auth.py, backend/app/api/router.py, backend/tests/test_auth.py, .planning/phases/02-company-access-and-tenant-foundation/02-CONTEXT.md</read_first>
  <action>Create a local `User` model keyed by Clerk user ID with the identity fields needed for Phase 2, add the corresponding migration, expose `/api/v1/auth/me`, and extend `backend/app/platform/webhooks.py` so verified Clerk `user.created` and `user.updated` events upsert local user rows while `user.deleted` deactivates or tombstones the corresponding local shadow row safely. Tests must prove missing/invalid authenticated request state returns 401, valid authenticated requests resolve the local user, unsigned webhook payloads are rejected before any user-row mutation occurs, and delete events do not leave active stale user records behind.</action>
  <verify>
    <automated>rtk docker compose run --rm backend pytest -q tests/test_auth.py && rtk docker compose run --rm backend alembic upgrade head</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/db/models/user.py` defines a Clerk-keyed local user shadow model
    - `backend/migrations/versions/0002_phase2_auth_users.py` creates the user table
    - `backend/app/api/routes/auth.py` exposes `/api/v1/auth/me`
    - `backend/app/platform/webhooks.py` handles verified `user.created`, `user.updated`, and `user.deleted` events
  </acceptance_criteria>
  <done>The backend can authenticate a Clerk user, persist a local user shadow row, and keep that row synchronized from verified Clerk user lifecycle events.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
- Clerk-managed identity vs. XAI-owned backend authorization
- Public webhook ingress vs. trusted Clerk/Svix-signed events
- Request-time DB session lifecycle vs. concurrent FastAPI request handling

## STRIDE Register
| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | Protected FastAPI auth dependency | mitigate | Verify only Clerk session-authenticated request state for protected routes and reject missing/invalid state with 401. |
| T-02-02 | Tampering | Clerk user webhook ingestion | mitigate | Verify webhook signatures before any local user-row mutation and keep upserts idempotent. |
| T-02-03 | Denial of Service | Backend DB/session layer | mitigate | Move request-time DB access to async engine/session primitives so authentication and future company routes do not block the event loop. |
</threat_model>

## Artifacts this phase produces
- `backend/app/platform/auth.py`
  - `AuthenticatedPrincipal`
  - Clerk verification dependency
- `backend/app/platform/webhooks.py`
  - verified user-webhook sync handler
- `backend/app/db/models/user.py`
  - `User`
- `backend/app/api/routes/auth.py`
  - `GET /api/v1/auth/me`

<verification>
Before declaring plan complete:
- [ ] `rtk docker compose run --rm backend pytest -q tests/test_auth.py`
- [ ] `rtk docker compose run --rm backend alembic upgrade head`
- [ ] `rtk make test-backend && rtk make test-frontend`
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Backend protected routes trust Clerk-authenticated request state
- Local user shadow rows are synced from verified Clerk user lifecycle events
</success_criteria>

<output>
After completion, create `.planning/phases/02-company-access-and-tenant-foundation/02-01-SUMMARY.md`
</output>
