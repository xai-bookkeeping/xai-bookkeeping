---
phase: 02-company-access-and-tenant-foundation
plan: 10
type: execute
wave: 10
depends_on:
  - "02-09"
files_modified:
  - frontend/src/routes/root.tsx
  - frontend/src/components/molecules/company-switcher.tsx
  - frontend/src/components/organisms/company-setup-required-state.tsx
  - frontend/src/test/workspace-shell.test.tsx
  - frontend/src/test/company-switcher.test.tsx
autonomous: true
gap_closure: true
requirements:
  - AUTH-05
  - COMP-02
  - COMP-04
  - COMP-05
  - SETUP-01
user_setup: []
must_haves:
  truths:
    - The authenticated shell distinguishes workspace ready, setup pending, and true forbidden company access instead of collapsing them into one error state.
    - First-run users with an active company remain in a calm setup handoff until readiness completes instead of being bounced into workspace early.
    - Company switching still clears cached data before the next company renders and never leaks previous-company records.
  artifacts:
    - `frontend/src/routes/root.tsx` branches on typed backend readiness instead of inferring setup from a generic 403.
    - `frontend/src/components/organisms/company-setup-required-state.tsx` presents owner-first setup-handoff content.
    - `frontend/src/components/molecules/company-switcher.tsx` preserves `setActive` plus cache-clearing semantics under the new shell flow.
    - Workspace-shell and company-switcher regression tests exist for the new states.
  key_links:
    - `frontend/src/routes/root.tsx` links bootstrap readiness to distinct ready, pending, and forbidden shell branches.
    - `frontend/src/components/molecules/company-switcher.tsx` links `setActive(...)` to `queryClient.clear()` before the next company render per D-17.
    - `frontend/src/components/organisms/company-setup-required-state.tsx` links backend `company_context_pending` responses to product-owned handoff copy instead of generic access-denied framing.
---

<objective>
Finish the shell-state follow-up for the Phase 2 gap plan set.

Purpose: even with a stable auth flow, the product still fails Phase 2 UAT if the authenticated shell cannot separate setup handoff from true access denial or if company switching leaks stale data.
Output: typed readiness-aware shell branches, revised company-setup handoff state, preserved company-switcher cache-clearing behavior, and focused workspace-shell regression tests.
</objective>

<execution_context>
@/Users/mali/Development/xai-bookkeeping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mali/Development/xai-bookkeeping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-company-access-and-tenant-foundation/02-CONTEXT.md
@.planning/phases/02-company-access-and-tenant-foundation/02-UAT.md
@.planning/phases/02-company-access-and-tenant-foundation/02-UI-SPEC.md
@.planning/phases/02-company-access-and-tenant-foundation/02-04-SUMMARY.md
@.planning/phases/02-company-access-and-tenant-foundation/02-07-SUMMARY.md
@xai-design.html
</context>

## Locked decisions carried forward

- `D-07` `D-08` `D-09`: keep explicit company-path scoping and true 403 foreign-company denial.
- `D-17` `D-18`: keep company switching in the header via Clerk `setActive(...)` and active-company hooks.

<tasks>
<task type="auto">
  <name>Task 1: Split workspace-ready, setup-pending, and forbidden shell states</name>
  <files>frontend/src/routes/root.tsx, frontend/src/components/organisms/company-setup-required-state.tsx, frontend/src/test/workspace-shell.test.tsx</files>
  <read_first>frontend/src/routes/root.tsx, frontend/src/components/organisms/company-setup-required-state.tsx, frontend/src/test/workspace-shell.test.tsx, .planning/phases/02-company-access-and-tenant-foundation/02-UAT.md, .planning/phases/02-company-access-and-tenant-foundation/02-UI-SPEC.md, xai-design.html</read_first>
  <action>Per `D-07`, `D-08`, and `D-09`, stop inferring setup from a generic forbidden response. Make `frontend/src/routes/root.tsx` wait for the typed bootstrap/readiness result before trusting company-scoped reads, branch separately for `ready`, `company_context_pending`, and true forbidden access, and keep the setup-handoff UI in `company-setup-required-state.tsx` aligned with the owner-first copy contract from `02-UI-SPEC.md`. The handoff state must feel like a deliberate continuation of company setup, not a disguised permission error. Cover the differentiated shell branches in `frontend/src/test/workspace-shell.test.tsx`.</action>
  <verify>
    <automated>rtk docker compose run --rm frontend npm run test -- --run src/test/workspace-shell.test.tsx src/test/company-switcher.test.tsx</automated>
  </verify>
  <done>The authenticated shell renders distinct ready, setup-pending, and forbidden contracts that match backend truth.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve company-switcher cache safety under the new readiness flow</name>
  <files>frontend/src/components/molecules/company-switcher.tsx, frontend/src/test/company-switcher.test.tsx</files>
  <read_first>frontend/src/components/molecules/company-switcher.tsx, frontend/src/test/company-switcher.test.tsx, .planning/phases/02-company-access-and-tenant-foundation/02-UAT.md, .planning/phases/02-company-access-and-tenant-foundation/02-UI-SPEC.md</read_first>
  <action>Keep the existing `02-04` switching guarantees while the shell moves to readiness-driven behavior. `frontend/src/components/molecules/company-switcher.tsx` must still hold the old company context until `setActive(...)` succeeds, run `queryClient.clear()` before the next company renders, and show the approved switching feedback instead of stale data or generic errors. Extend `frontend/src/test/company-switcher.test.tsx` so it proves stale company data is cleared and the switcher still behaves correctly when the next company enters a setup-pending handoff.</action>
  <verify>
    <automated>rtk docker compose run --rm frontend npm run test -- --run src/test/workspace-shell.test.tsx src/test/company-switcher.test.tsx</automated>
  </verify>
  <done>The company switcher remains safe and deterministic even when the next company is not yet fully ready.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend readiness contract -> shell rendering | The shell must not treat setup-pending as ready or as foreign-company denial |
| Company switch -> cached browser data | Previous-company query data must be removed before new scoped content renders |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10-01 | Information Disclosure | workspace shell states | mitigate | Map backend `company_context_pending` to a setup handoff state and reserve 403 rendering for real forbidden access only. |
| T-02-10-02 | Information Disclosure | company switcher cache | mitigate | Preserve `setActive(...)` plus `queryClient.clear()` before new company rendering and hold that behavior under regression tests. |
</threat_model>

<verification>
- [ ] `rtk docker compose run --rm frontend npm run test -- --run src/test/workspace-shell.test.tsx src/test/company-switcher.test.tsx`
- [ ] `rtk make test-backend`
- [ ] `rtk make test-frontend`
</verification>

<success_criteria>
- The shell separates ready, setup-pending, and forbidden company states.
- First-run users are held in a clear setup handoff until readiness completes.
- Company switching still clears stale data before the next company renders.
</success_criteria>

<output>
Create `.planning/phases/02-company-access-and-tenant-foundation/02-10-SUMMARY.md` when done
</output>
