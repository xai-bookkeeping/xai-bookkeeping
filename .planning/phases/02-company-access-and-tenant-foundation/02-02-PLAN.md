---
phase: 02-company-access-and-tenant-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - frontend/package.json
  - frontend/vite.config.ts
  - frontend/src/main.tsx
  - frontend/src/app/router.tsx
  - frontend/src/routes/root.tsx
  - frontend/src/routes/auth/sign-in.tsx
  - frontend/src/routes/auth/create-company.tsx
  - frontend/src/api/
  - frontend/src/test/auth-routes.test.tsx
autonomous: true
requirements:
  - AUTH-01
  - AUTH-05
  - SETUP-01
user_setup: []
must_haves:
  truths:
    - Signed-out users are redirected into the Clerk-backed sign-in flow before protected workspace routes render.
    - Protected frontend API calls use a same-origin `/api` contract that preserves httpOnly Clerk cookies.
    - First-run users without an active company have a clear company-creation entry point instead of landing in a dead-end shell.
    - The frontend continues consuming generated API clients rather than manual fetch contracts.
  artifacts:
    - `ClerkProvider` is wired into the frontend root.
    - A sign-in route exists and matches the approved UI contract.
    - The auth shell includes a Clerk-backed first-company creation entry and handoff for empty-company users.
    - Vite dev proxy and generated API client configuration use a same-origin `/api` base path.
  key_links:
    - Clerk browser session state is linked to protected React Router gates.
    - The first-company creation route is linked to Clerk organization creation and the backend/company bootstrap flow that materializes a selectable local company after `organization.created`.
    - Generated frontend API client calls are linked to the same-origin `/api` contract.
---

<objective>
Build the frontend auth shell and preserve the locked cookie-only Clerk transport contract.

Purpose: Phase 2 needs a real sign-in experience and a browser-to-API contract that keeps Clerk session handling in httpOnly cookies instead of leaking into frontend JS helpers.
Output: Clerk React provider wiring, protected routing, sign-in screen, first-company creation entry/handoff route, same-origin `/api` Vite proxy contract, regenerated client configuration, and frontend auth tests.
</objective>

<execution_context>
@/Users/mali/Development/xai-bookkeeping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mali/Development/xai-bookkeeping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-company-access-and-tenant-foundation/02-CONTEXT.md
@.planning/phases/02-company-access-and-tenant-foundation/02-RESEARCH.md
@.planning/phases/02-company-access-and-tenant-foundation/02-UI-SPEC.md
@frontend/package.json
@frontend/vite.config.ts
@frontend/src/main.tsx
@frontend/src/app/router.tsx
@frontend/src/routes/root.tsx
@xai-design.html
</context>

<tasks>
<task type="auto">
  <name>Task 1: Wire Clerk into the frontend root and protected router</name>
  <files>frontend/package.json, frontend/src/main.tsx, frontend/src/app/router.tsx, frontend/src/routes/root.tsx, frontend/src/routes/auth/sign-in.tsx, frontend/src/routes/auth/create-company.tsx</files>
  <read_first>frontend/package.json, frontend/src/main.tsx, frontend/src/app/router.tsx, frontend/src/routes/root.tsx, frontend/src/routes/auth/sign-in.tsx, frontend/src/routes/auth/create-company.tsx, .planning/phases/02-company-access-and-tenant-foundation/02-UI-SPEC.md, xai-design.html</read_first>
  <action>Add `@clerk/clerk-react`, wrap the app in `ClerkProvider`, introduce dedicated sign-in and create-company routes, and gate protected workspace routes with Clerk signed-in/signed-out primitives. The sign-in screen must follow the approved split layout and copy contract from `02-UI-SPEC.md`. For authenticated users with no active organization, route them into a Clerk-backed company creation entry flow that collects the initial company name, invokes Clerk organization creation, and then renders a safe pending handoff state until the backend mirror materializes and the new company becomes active. Do not claim settings editing here; that remains in the later settings plans.</action>
  <verify>
    <automated>rtk docker compose run --rm frontend npm run test -- --run src/test/auth-routes.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/main.tsx` contains `ClerkProvider`
    - `frontend/src/app/router.tsx` routes signed-out users to the sign-in flow before protected workspace content renders
    - `frontend/src/routes/auth/sign-in.tsx` matches the approved login shell structure and copy contract
    - `frontend/src/routes/auth/create-company.tsx` invokes Clerk organization creation and renders a pending handoff state
    - empty-company users are redirected into company creation rather than a blank protected shell
  </acceptance_criteria>
  <done>The frontend renders a Clerk-backed sign-in flow, enforces signed-in route protection, and gives first-run users a company-creation entry plus backend-handoff state.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve httpOnly-cookie transport through the same-origin `/api` contract</name>
  <files>frontend/vite.config.ts, frontend/src/api/, frontend/src/test/auth-routes.test.tsx</files>
  <read_first>frontend/vite.config.ts, frontend/src/api/, frontend/src/test/auth-routes.test.tsx, .planning/phases/02-company-access-and-tenant-foundation/02-CONTEXT.md, .planning/phases/02-company-access-and-tenant-foundation/02-RESEARCH.md, Makefile</read_first>
  <action>Route frontend API calls through a same-origin `/api` base path, configure Vite to proxy `/api` to the backend in development, regenerate the committed client under `frontend/src/api/`, and update the auth-route tests so they assert the protected shell relies on the same-origin API contract rather than a JS token helper. Include `rtk make gen-types` in the verify path so the generated client remains the source of truth for frontend/backend contracts.</action>
  <verify>
    <automated>rtk make gen-types && rtk docker compose run --rm frontend npm run test -- --run src/test/auth-routes.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/vite.config.ts` proxies `/api` to the backend
    - generated client files under `frontend/src/api/` are refreshed after `rtk make gen-types`
    - no new frontend helper reads Clerk session tokens directly from JS
  </acceptance_criteria>
  <done>The frontend preserves the locked same-origin cookie transport contract and keeps using generated API clients.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
- Clerk browser session cookies vs. frontend route/render logic
- Frontend same-origin `/api` path vs. backend container origin in development

## STRIDE Register
| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Information Disclosure | Frontend auth transport | mitigate | Preserve the httpOnly-cookie contract by using same-origin `/api` routing instead of JS-readable bearer-token helpers. |
| T-02-05 | Spoofing | Protected frontend routes | mitigate | Use Clerk signed-in/signed-out gates so signed-out users never render protected workspace routes. |
</threat_model>

## Artifacts this phase produces
- `frontend/src/routes/auth/sign-in.tsx`
  - sign-in route component
- `frontend/src/main.tsx`
  - Clerk provider wiring
- `frontend/vite.config.ts`
  - same-origin `/api` proxy contract

<verification>
Before declaring plan complete:
- [ ] `rtk make gen-types`
- [ ] `rtk docker compose run --rm frontend npm run test -- --run src/test/auth-routes.test.tsx`
- [ ] `rtk make test-backend && rtk make test-frontend`
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Frontend sign-in and route protection are Clerk-backed
- First-run users can start first-company creation through the product and hand off cleanly into the newly active workspace
- Same-origin `/api` contract preserves the locked cookie-only transport decision
</success_criteria>

<output>
After completion, create `.planning/phases/02-company-access-and-tenant-foundation/02-02-SUMMARY.md`
</output>
