---
phase: 02-company-access-and-tenant-foundation
plan: 08
type: execute
wave: 8
depends_on:
  - "02-07"
files_modified:
  - backend/app/api/routes/auth.py
  - backend/app/platform/clerk_organizations.py
  - backend/app/platform/company_access.py
  - backend/app/platform/webhooks.py
  - backend/tests/test_auth.py
  - backend/tests/test_company_authorization.py
  - backend/tests/test_role_permissions.py
  - backend/tests/test_audit_events.py
autonomous: true
gap_closure: true
requirements:
  - AUTH-01
  - AUTH-05
  - COMP-01
  - COMP-03
  - COMP-05
user_setup: []
must_haves:
  truths:
    - A first-time authenticated user can learn whether their active company is ready, still materializing, or absent without being treated as unauthorized.
    - Foreign-company access still fails with 403 even while first-run bootstrap behavior is added.
    - Team-role and audit behavior do not regress when shared Clerk organization and webhook paths are extended for bootstrap readiness.
  artifacts:
    - `backend/app/api/routes/auth.py` exposes a typed bootstrap/readiness contract.
    - `backend/app/platform/company_access.py` distinguishes setup-pending from true forbidden access.
    - Shared Clerk organization and webhook helpers support idempotent first-run company materialization.
    - Backend regression tests cover auth, company authorization, role permissions, and audit events together.
  key_links:
    - Bootstrap readiness is linked from `backend/app/api/routes/auth.py` to the active Clerk organization only, per D-04 and D-05.
    - Company access denial is linked to `backend/app/platform/company_access.py` and preserves 403 for real foreign-company requests per D-07, D-08, and D-09.
    - Shared Clerk organization and webhook helpers remain linked to role and audit flows through regression coverage.
---

<objective>
Close the backend half of the Phase 2 onboarding gap without reopening the tenant-security or team/audit foundations.

Purpose: the remaining frontend work needs a deterministic server contract for first-run company readiness, but that contract must preserve Clerk Organizations as the source of truth and must not weaken company isolation.
Output: a typed auth bootstrap endpoint, a setup-pending company-access contract, shared Clerk reconciliation helpers, and backend regression coverage across auth, tenant access, roles, and audit behavior.
</objective>

<execution_context>
@/Users/mali/Development/xai-bookkeeping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mali/Development/xai-bookkeeping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-company-access-and-tenant-foundation/02-CONTEXT.md
@.planning/phases/02-company-access-and-tenant-foundation/02-UAT.md
@.planning/phases/02-company-access-and-tenant-foundation/02-03-SUMMARY.md
@.planning/phases/02-company-access-and-tenant-foundation/02-05-SUMMARY.md
@backend/app/api/routes/auth.py
@backend/app/platform/clerk_organizations.py
@backend/app/platform/company_access.py
@backend/app/platform/webhooks.py
</context>

## Locked decisions carried forward

- `D-01` `D-02` `D-03`: keep Clerk-backed auth, httpOnly-cookie transport, and the async SQLAlchemy path.
- `D-04` `D-05` `D-06`: keep Clerk Organizations plus Postgres shadow rows as the durable company model; bootstrap may reconcile only the authenticated principal's active org.
- `D-07` `D-08` `D-09`: keep explicit company-path authorization and preserve 403 for real foreign-company access.
- `D-13` `D-14` `D-15`: do not break existing audit-event semantics while extending shared auth and organization helpers.

<tasks>
<task type="auto">
  <name>Task 1: Add the backend bootstrap and setup-pending readiness contract</name>
  <files>backend/app/api/routes/auth.py, backend/app/platform/clerk_organizations.py, backend/app/platform/company_access.py, backend/app/platform/webhooks.py</files>
  <read_first>backend/app/api/routes/auth.py, backend/app/platform/clerk_organizations.py, backend/app/platform/company_access.py, backend/app/platform/webhooks.py, .planning/phases/02-company-access-and-tenant-foundation/02-CONTEXT.md, .planning/phases/02-company-access-and-tenant-foundation/02-UAT.md, .planning/phases/02-company-access-and-tenant-foundation/02-03-SUMMARY.md, .planning/phases/02-company-access-and-tenant-foundation/02-05-SUMMARY.md</read_first>
  <action>Per `D-04`, `D-05`, `D-07`, `D-08`, and `D-09`, add `GET /api/v1/auth/bootstrap` or an equivalent typed auth-readiness endpoint that reports `no_active_company`, `company_context_pending`, or `ready` for the authenticated principal only. Extend the existing Clerk organization helper only far enough to read the principal's active organization and membership when local shadow rows are missing, and route any reconciliation through the existing webhook-oriented upsert path so bootstrap and Clerk webhooks share one materialization flow instead of creating a second source of truth. Update `backend/app/platform/company_access.py` so same-org requests with missing local user, company, or membership rows return a stable setup-pending contract such as HTTP 409 with a code like `company_context_pending`, while true foreign-company requests still short-circuit to 403 before any local company lookup.</action>
  <verify>
    <automated>rtk docker compose run --rm backend pytest -q tests/test_auth.py tests/test_company_authorization.py tests/test_role_permissions.py tests/test_audit_events.py</automated>
  </verify>
  <done>Backend auth and company access can describe first-run readiness without weakening tenant isolation or Clerk-as-source-of-truth decisions.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the shared auth, organization, role, and audit regressions with one backend suite</name>
  <files>backend/tests/test_auth.py, backend/tests/test_company_authorization.py, backend/tests/test_role_permissions.py, backend/tests/test_audit_events.py</files>
  <read_first>backend/tests/test_auth.py, backend/tests/test_company_authorization.py, backend/tests/test_role_permissions.py, backend/tests/test_audit_events.py, .planning/phases/02-company-access-and-tenant-foundation/02-UAT.md</read_first>
  <action>Expand backend regression coverage so the new bootstrap contract proves the `no_active_company`, `company_context_pending`, `ready`, and true foreign-company 403 branches, and so shared-path changes do not break Clerk-backed membership lifecycle or audit behavior. The regression suite must keep `backend/tests/test_role_permissions.py` proving allow and deny behavior across membership and protected resource actions, and keep `backend/tests/test_audit_events.py` proving existing audit-event writes and company scoping remain intact while auth/bootstrap helpers change.</action>
  <verify>
    <automated>rtk docker compose run --rm backend pytest -q tests/test_auth.py tests/test_company_authorization.py tests/test_role_permissions.py tests/test_audit_events.py</automated>
  </verify>
  <done>The backend gap-closure contract is covered by the auth, tenant, role, and audit regression suite required for safe execution.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Clerk session -> bootstrap endpoint | Only the authenticated principal's active Clerk organization may influence first-run readiness |
| Requested `company_id` -> company access guard | Foreign-company requests must stay forbidden even when local rows are missing |
| Shared Clerk helpers -> team/audit flows | Bootstrap changes must not break existing membership authorization or audit emission paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08-01 | Elevation of Privilege | bootstrap endpoint | mitigate | Limit reconciliation to the authenticated principal and active organization only; reject arbitrary company bootstrap from the client. |
| T-02-08-02 | Information Disclosure | company access contract | mitigate | Preserve true foreign-company denial with 403 and use a separate setup-pending contract only when the active org matches the requested company. |
| T-02-08-03 | Tampering | shared Clerk webhook and organization helpers | mitigate | Reuse idempotent webhook materialization paths and run auth, role-permission, and audit regression tests together before completion. |
</threat_model>

<verification>
- [ ] `rtk docker compose run --rm backend pytest -q tests/test_auth.py tests/test_company_authorization.py tests/test_role_permissions.py tests/test_audit_events.py`
- [ ] `rtk make test-backend`
- [ ] `rtk make test-frontend`
</verification>

<success_criteria>
- Auth bootstrap returns typed `no_active_company`, `company_context_pending`, or `ready` results for the authenticated principal.
- Company access keeps 403 for real foreign-company requests while exposing a separate setup-pending contract for active-org first-run cases.
- Shared Clerk organization and webhook changes do not regress role-permission or audit-event behavior.
</success_criteria>

<output>
Create `.planning/phases/02-company-access-and-tenant-foundation/02-08-SUMMARY.md` when done
</output>
